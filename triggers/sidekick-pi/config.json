{
  "name": "Sidekick - Pi",
  "description": "When Tuple Capture starts, launches Pi as an active live-call companion via `tuple connect`. A bundled extension follows the triggering recording with `tuple capture next`, summarizes as it goes, surfaces the watch state on Pi's toolbar, and adds a set_watch_mode tool.",
  "category": "Sidekick",
  "platforms": ["macos"],
  "language": "bash"
}
